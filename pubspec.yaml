name: flutter_puzzle
description: flutter_puzzle
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^5.0.0
  rxdart: ^0.26.0
  freezed_annotation: ^0.14.1

dev_dependencies:
  test: ^1.17.2
  build_runner: ^2.0.1
  freezed: ^0.14.1+3

flutter:
  uses-material-design: true

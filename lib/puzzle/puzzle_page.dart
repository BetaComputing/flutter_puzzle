import 'package:flutter/material.dart';

/// パズルゲームのページ
class PuzzlePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('flutter_puzzle')));
}
